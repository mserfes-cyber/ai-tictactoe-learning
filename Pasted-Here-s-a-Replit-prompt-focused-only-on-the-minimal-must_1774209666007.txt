Here’s a Replit prompt focused only on the **minimal / must‑have** changes:

> You are updating an educational Tic-Tac-Toe AI web app for a 4th grade science fair. The core game and AI logic already work well. Your job is to make small, kid‑friendly UI changes without breaking any existing functionality.
>
> Current app (do NOT change the logic/flow):
> - Full clickable Tic-Tac-Toe board (❌ vs ⭕) and working game loop.
> - Auto-training on first load (5 games with progress bar, “AI Ready” popup).
> - 5-game progression: games 1–5 use a minimax AI, later games use a combined KNN + Q-Learning AI.
> - “SUPER MACHINE LEARNING MODE” badge after training.
> - Milestone popups, win/loss/draw banners, auto-reset, “TRAIN AI 5 MORE” button.
> - “AI Battleground” scoreboard/log (keep this name).
> - Game History & AI Memory viewers.
> - Sidebar Q&A agent and footer.
>
> Implement ONLY these minimal, must‑have changes:
>
> 1. Use “AI” instead of “robot” and hide jargon from kids
>    - In all kid‑facing UI text (labels, popups, headings), make sure we use **AI** instead of “robot”.
>    - Replace or simplify any visible references like “minimax AI”, “KNN”, or “Q-Learning” in main UI text:
>      - For example, use:
>        - “Basic AI Brain (follows rules)” instead of “minimax AI” in kid-facing labels.
>        - “Learning AI Brain (learns from games)” instead of explicitly saying “KNN + Q-Learning”.
>    - You do NOT need to remove those technical terms from the code or internal comments; just ensure they are not shown prominently to kids in buttons/headings/toasts.
>
> 2. Add the science question visibly in the app
>    - Add a small, always-visible text area (for example in a top banner, header, or side panel) showing:
>      - Title: “Science Question”
>      - Text: “Can AI learn from every game that I play?”
>    - This should be clearly readable during gameplay but not cover the board.
>
> 3. Add a simple “Explain how AI remembers” help popup
>    - Add a small button or link somewhere near the board or sidebar, labeled:
>      - “Explain how AI remembers”
>    - When clicked, show a short popup/modal/toast with a kid‑friendly explanation (2–3 short sentences), such as:
>      - “AI remembers which moves help it win.”
>      - “After each game, AI updates its memory.”
>      - “Next time, it tries to use the moves that worked better.”
>    - Make this explanation stay visible for a few seconds (for example 5–6 seconds) or until closed by the user.
>
> 4. Keep “AI Battleground” as the main name
>    - Do NOT rename the “AI Battleground” element.
>    - You may optionally add a short subtitle under it like “Game results (what our AI learned)”, but the main title must remain “AI Battleground”.
>
> 5. Preserve existing behavior
>    - Do not change the AI training logic, game rules, or progression.
>    - Only adjust UI text/labels and add the small science question and explanation elements described above.
>
> When you are done:
> - List which files and components you changed.
> - Point out where in the code the science question text, the “Explain how AI remembers” button, and its popup content are defined so they can be updated later if needed.